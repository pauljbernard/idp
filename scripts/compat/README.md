Compatibility-only verifiers and helpers.

These scripts were carried over from the broader source environment and still target route families or auth/session helpers that the standalone IDP server in this repo does not implement.

They are kept here as reference material, not as the primary standalone verification surface.
